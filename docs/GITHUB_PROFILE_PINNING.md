# GitHub Profile Pinning

GitHub profile pins are edited manually in the profile UI. Use this order so
the profile points to the current method library before older support slices.

## Recommended Pins

1. `baishiqi45-dotcom/baishiqi45-dotcom`
   - Headline identity: AI video gates, agent handoff templates, Project
     Clinic, workflow rescue, and video admission.
2. `baishiqi45-dotcom/mmi-gateway`
   - Supporting proof slice, not the headline: multimodal intake into
     review-required candidate evidence.
3. `baishiqi45-dotcom/codex-sidecar-subagents`
   - Supporting proof slice, not the headline: bounded read-only advisors with
     Codex-owned verification.
4. `baishiqi45-dotcom/epistemic-os`
   - Supporting proof slice, not the headline: claim-scope checks for candidate
     evidence.

Optional pin:

- `baishiqi45-dotcom/netfix`
  - Keep only if a practical utility slot is useful. It is an operator downtime
    tool, not part of the personal identity headline.

## Manual Steps

1. Open the GitHub profile.
2. Click `Customize your pins`.
3. Put this repository first.
4. Keep older repositories as support slices, not the headline.
5. Recheck descriptions after pinning so each repo has a current one-line role.

Current audit note, 2026-07-08: the live profile pins show
`codex-sidecar-subagents`, `epistemic-os`, `mmi-gateway`, and `netfix`, but this
profile repository is not pinned. Fixing that is the highest-impact manual
profile action because the profile repo is the only page that explains the
current identity and routes the support repos.

## Visual Identity Checklist

Manual GitHub profile actions:

1. Upload `assets/avatar-abstract-mark-1024.png` as the GitHub account avatar.
   It is an abstract white-field / gate mark, not a portrait and not a camera
   icon. The older portrait/camera pixel identity was removed from the active
   asset set because it collided with the site direction and read weaker at
   small sizes.
2. Keep this repository pinned first.
3. Upload `assets/social-card.jpg` as this repository's social preview image.
4. Use supporting repos only as proof slices below the method-library headline.

## Account Profile Fields

The current GitHub token does not have the `user` scope required for API updates,
so these are manual profile edits:

```text
Website:
https://baishiqi45-dotcom.github.io/baishiqi45-dotcom/

Bio:
Film-trained AI production control: AI video gates, agent handoffs, Project Clinic, workflow rescue.
```

## Topic Suggestions

Use public discovery topics that describe the current work:

```text
ai-video
ai-video-production
short-drama
film-direction
agent-workflows
project-clinic
artifact-review
artifact-verification
non-engineer-tools
product-ops
```
